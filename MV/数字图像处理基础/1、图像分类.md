# 图像与数字图像
数字图像：数字图像可用矩阵或数组进行描述
- 黑白图像：每个像素只能是黑或者白，没有中间过渡，也称为二值图像
- 灰度图像：每个像素的信息由一个量化的灰度级来描述，没有彩色信息（灰度取值0~255）0表示纯黑色，255表示纯白色。中间的数字表示黑白之间的过渡色
- 彩色图像：每个像素的信息由RGB三原色构成，其中RGB由不同的灰度级描述
- 索引图像（伪彩色图像）：图像的像素值比较少，可以将所有像素值整合成一个列表，以索引方式取像素值
